/// <reference types="node" />
export declare class TdProxy {
    private client;
    constructor({ command }: {
        command?: string;
    });
    create(): Buffer;
    destroy(client: Buffer): void;
    execute(client: Buffer, query: string): string;
    receive(client: Buffer, timeout: number): Promise<string | null>;
    send(client: Buffer, query: string): Promise<void>;
}
