import { ApiRequest, PlainObjectToModelTransformer, TdObject, TdProvider as BaseTdProvider } from '@airgram/core';
import { TdClientConfig } from './TdClient';
export declare type TdProviderConfig = Omit<TdClientConfig, 'handleUpdate' | 'handleError' | 'models'>;
export declare class TdProvider extends BaseTdProvider {
    private client;
    private readonly config;
    private waitForDestroy;
    constructor(config?: TdProviderConfig);
    destroy(): Promise<void>;
    execute(request: ApiRequest): TdObject;
    initialize(handleUpdate: (update: TdObject) => Promise<unknown>, handleError: (error: Error | string) => void, models?: PlainObjectToModelTransformer): void;
    pause(): void;
    resume(): void;
    send(request: ApiRequest): Promise<TdObject>;
}
