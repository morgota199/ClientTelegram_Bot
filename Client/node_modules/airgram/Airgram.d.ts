import { Airgram as BaseAirgram, Config } from '@airgram/core';
import { TdProvider, TdProviderConfig } from './components';
export interface AirgramConfig extends Omit<Config, 'provider'>, TdProviderConfig {
}
export declare class Airgram extends BaseAirgram<TdProvider> {
    constructor(config: AirgramConfig);
}
